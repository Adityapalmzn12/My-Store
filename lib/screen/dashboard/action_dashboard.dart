import 'package:flutter/material.dart';
class ActionDashboard extends StatelessWidget {
  const ActionDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Coming Soon"),),
    );
  }
}
